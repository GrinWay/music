input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "service" => "music-app" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "music-logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}
